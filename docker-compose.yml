version: '1.0'
services:
  fastapi:
    build: .
    ports:
      - 8003:8003
      - 5679:5679
    volumes:
      - .:/code
    command: uvicorn src.main:app --host 0.0.0.0 --port 8003 --reload
    restart: always